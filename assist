import sqlite3
connection = sqlite3.connect("server.db")
crsr = connection.cursor()

sql_command = """CREATE TABLE windowErrors (
    windowName varchar
    time int
    error varchar

)"""

crsr.execute(sql_command)
connection.commit()
connection.close()
