import sqlite3
connection = sqlite3.connect("server.db")
crsr = connection.cursor()

sql_command = """ALTER TABLE user
RENAME TO users"""

crsr.execute(sql_command)
connection.commit()
connection.close()